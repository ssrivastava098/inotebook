import { createContext } from "react";

const authContext = createContext(); //Syntax to create a new context
//This contex will hold all of our states related to our notes

export default authContext